import { LandingSection } from "@/components/public/landing-section";

export default function GuidesPage() {
  return (
    <LandingSection
      eyebrow="Guides"
      title="Field guides"
      body="Operational guides for indie app founders: what to set up, what to skip, and what to revisit as the app grows."
    />
  );
}
