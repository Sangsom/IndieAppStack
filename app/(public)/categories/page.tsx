import { LandingSection } from "@/components/public/landing-section";

export default function CategoriesPage() {
  return (
    <LandingSection
      eyebrow="Categories"
      title="Tool categories"
      body="Browse the stack by jobs to be done: analytics, payments, backend, support, automation, design, and distribution."
    />
  );
}
