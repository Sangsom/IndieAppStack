import { LandingSection } from "@/components/public/landing-section";

export default function ToolsPage() {
  return (
    <LandingSection
      eyebrow="Tools"
      title="Tools directory"
      body="A curated index of the products solo mobile developers use to ship, measure, monetize, and support their apps."
    />
  );
}
